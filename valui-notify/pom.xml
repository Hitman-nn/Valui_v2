<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.valui</groupId>
        <artifactId>valui-parent</artifactId>
        <version>2.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>valui-notify</artifactId>
    <name>Valui :: Notify</name>
    <description>Kafka consumer, subscription filtering, Telegram notification dispatch</description>

    <dependencies>
        <dependency>
            <groupId>com.valui</groupId>
            <artifactId>valui-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.valui</groupId>
            <artifactId>valui-bot</artifactId>
        </dependency>
        <dependency>
            <groupId>com.valui</groupId>
            <artifactId>valui-user</artifactId>
        </dependency>
        <dependency>
            <groupId>com.valui</groupId>
            <artifactId>valui-betting</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- Kafka consumer -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
        <!-- Redis: TelegramRateLimiter (also transitively available via valui-user) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>