-- Флаг «алерт о низком балансе уже отправлен».
-- Позволяет разделить пользовательский порог (tokenLowThreshold)
-- и состояние дедупликации уведомлений.
-- Сбрасывается при восстановлении баланса выше порога и при ежемесячном гранте.
ALTER TABLE users ADD COLUMN token_alert_sent BOOLEAN NOT NULL DEFAULT FALSE;
